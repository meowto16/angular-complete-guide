# Пробрасываем данные рецепта при помощи связки свойств

1. Выносим в отдельный компонент `app-recipe-item`
2. Пробегаемся `*ngFor` по списку и рендерим элементы.
3. Пробрасываем в него данные `[recept]="recept"`
